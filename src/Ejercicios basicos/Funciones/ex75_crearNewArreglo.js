// Ejercicio 75
// Crear un new arreglo taking el medio elements de el dos arrays de enteros, each de length 3.

function middleElements(a, b) {
  return [a[1], b[1]];
}
console.log(middleElements([1, 2, 3], [4, 5, 6])); // [2, 5]
